# Environment properties for Allure Report
# Filtered by Maven during build
Project=${allure.env.project}
Author=${allure.env.user}
Environment=${allure.env.env}
Browser=${allure.env.browser}
OS=${os.name}
Java_Version=${java.version}
Maven_Version=${maven.version}
